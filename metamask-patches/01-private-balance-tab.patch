diff --git a/ui/components/app/assets/private-balance-tab/private-balance-tab.tsx b/ui/components/app/assets/private-balance-tab/private-balance-tab.tsx
index 3607083..30a978f 100644
--- a/ui/components/app/assets/private-balance-tab/private-balance-tab.tsx
+++ b/ui/components/app/assets/private-balance-tab/private-balance-tab.tsx
@@ -761,23 +761,24 @@ export default function PrivateBalanceTab() {
   const [refetchTick, setRefetchTick] = useState(0);
   const [activity, setActivity] = useState<TransferEvent[] | 'loading' | null>(null);
 
-  useEffect(() => {
-    if (!ownerAddress) return;
-    let cancelled = false;
-    setActivity('loading');
-    (async () => {
-      try {
-        const events = await fetchRecentTransfers(
-          ownerAddress,
-          NOX_CTOKENS.map((t) => t.address),
-        );
-        if (!cancelled) setActivity(events);
-      } catch {
-        if (!cancelled) setActivity([]);
-      }
-    })();
-    return () => { cancelled = true; };
-  }, [ownerAddress, refetchTick]);
+  // TEMPORARILY DISABLED — bisecting activity crash
+  // useEffect(() => {
+  //   if (!ownerAddress) return;
+  //   let cancelled = false;
+  //   setActivity('loading');
+  //   (async () => {
+  //     try {
+  //       const events = await fetchRecentTransfers(
+  //         ownerAddress,
+  //         NOX_CTOKENS.map((t) => t.address),
+  //       );
+  //       if (!cancelled) setActivity(events);
+  //     } catch {
+  //       if (!cancelled) setActivity([]);
+  //     }
+  //   })();
+  //   return () => { cancelled = true; };
+  // }, [ownerAddress, refetchTick]);
 
   useEffect(() => {
     if (!ownerAddress) return;
